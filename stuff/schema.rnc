default namespace = ""

start = element root { p+ }
p =
  element p {
    (text
     | element hi {
         attribute rendition { xsd:NMTOKEN },
         text
       }
     | element note { p })+
  }
